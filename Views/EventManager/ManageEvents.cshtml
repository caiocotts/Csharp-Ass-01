@using Assignment01.Util
@model List<Event>

@{
    ViewData["Title"] = "Personal Event Management";

    var sortOrder = ViewData["SortOrder"] as string ?? string.Empty;
    var categoryFilter = ViewData["CategoryFilter"] as string ?? string.Empty;
    var availabilityFilter = ViewData["AvailabilityFilter"] as string ?? "all";
    var searchString = ViewData["SearchString"] as string ?? string.Empty;
}
<style>
    .table th a.sort-link {
        color: inherit;
        text-decoration: none;
    }

    .table th a.sort-link:hover {
        color: inherit;
        text-decoration: none;
    }

    .sort-arrow {
        font-size: 0.7rem;
        line-height: 1;
    }
</style>

<h2>Manage Events</h2>
<br/>

<form asp-controller="EventManager" asp-action="ManageEvents" method="get">
    <p>
        <select class="form-select"
                name="CategoryFilter"
                style="width: auto; display: inline-block; margin-right: 10px;">
            <option value="">All Categories</option>
            @{
                var categories = new List<string> { "Webinar", "Concert", "Workshop", "Conference" };
                foreach (var category in categories)
                {
                    if (string.Equals(category, categoryFilter, StringComparison.OrdinalIgnoreCase))
                    {
                        <option value="@category" selected>@category</option>
                    }
                    else
                    {
                        <option value="@category">@category</option>
                    }
                }
            }
        </select>
        <label>
            <input type="text" name="searchString" value="@ViewData["CurrentFilter"]"/>
        <select
            class="form-select"
            name="AvailabilityFilter"
            style="width: auto; display: inline-block; margin-right: 10px;">
            @{
                var availabilityOptions = new List<string> { "All", "Available", "Sold Out" };
                foreach (var availability in availabilityOptions)
                {
                    if (string.Equals(availabilityFilter, availability, StringComparison.OrdinalIgnoreCase))
                    {
                        <option value="@availability" selected>@availability</option>
                    }
                    else
                    {
                        <option value="@availability">@availability</option>
                    }
                }
            }
        </select>
        <label style="margin-right: 10px;">
            <input type="text" name="searchString" value="@searchString" placeholder="Search by title"/>
        </label>
        </label>
        <input type="submit" value="Search" class="btn btn-info"/>
        <a asp-action="ManageEvents" class="btn btn-secondary" style="margin-left:10px;">Reset</a>
    </p>
    <input type="hidden" name="sortOrder" value="@sortOrder"/>
</form>
<br/>
<a asp-action="Create" class="btn btn-primary">Create New Event</a>
<br/><br/>

@{
    var tableHeaders = new[]
    {
        (Value: "ID", SortAsc: "", SortDesc: "id_desc"),
        (Value: "Title", SortAsc: "title", SortDesc: "title_desc"),
        (Value: "Category", SortAsc: "category", SortDesc: "category_desc"),
        (Value: "Event Date", SortAsc: "date", SortDesc: "date_desc"),
        (Value: "Price per Ticket", SortAsc: "price", SortDesc: "price_desc"),
        (Value: "Available Tickets", SortAsc: "tickets", SortDesc: "tickets_desc")
    };

    if (Model.Any())
    {
        <table class="table">
            <thead>
            <tr>
                @foreach (var tableHeader in tableHeaders)
                {
                    <th>
                        <a class="sort-link"
                           asp-action="ManageEvents"
                           asp-route-sortOrder="@(sortOrder == tableHeader.SortAsc ? tableHeader.SortDesc : tableHeader.SortAsc)"
                           asp-route-searchString="@searchString"
                           asp-route-category="@categoryFilter">
                           asp-route-categoryFilter="@categoryFilter"
                           asp-route-availabilityFilter="@availabilityFilter"
                            @tableHeader.Value
                            <span class="sort-arrow">
                                @(SortArrow.Arrow(sortOrder, tableHeader.SortAsc, tableHeader.SortDesc))
                            </span>
                        </a>
                    </th>
                }
            </tr>
            </thead>
            <tbody>
            @foreach (var anEvent in Model)
            {
                var availableTickets = anEvent.AvailableTickets switch
                {
                    0 => "SOLD OUT",
                    (< 200) => $"{anEvent.AvailableTickets} | ALMOST GONE!", // removing the "redundant parentheses" will cause the build to fail, so don't do that pls
                    _ => $"{anEvent.AvailableTickets}"
                };

                <tr>
                    <td>@anEvent.Id</td>
                    <td>@anEvent.Title</td>
                    <td>@anEvent.Category</td>
                    <td>@anEvent.EventDate</td>
                    <td>$@anEvent.PricePerTicket</td>
                    <td>@availableTickets</td>
                    <td>
                        <a asp-action="Edit" asp-route-id="@anEvent.Id" class="btn btn-sm btn-warning me-2">
                            Edit
                        </a>

                        <a asp-action="DeleteConfirmation" asp-route-id="@anEvent.Id" class="btn btn-danger">
                            Delete
                        </a>
                    </td>
                </tr>
            }
            </tbody>
        </table>
    }
    else
    {
        <p>No events available.</p>
    }
}